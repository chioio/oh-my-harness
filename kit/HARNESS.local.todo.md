# 本地补充占位

此文件表示当前项目的 harness 仍需继续补充。

## agent 行为要求

如果检测到此文件存在：

1. 不要把当前项目视为已完成本地 harness 配置
2. 继续读取 `AGENTS.md`，若存在则继续读取 `AGENTS.local.md`，并读取 `_harness/.setup/` 中的相关说明
3. 通过对话收集当前项目的实际信息
4. 补全本地 `_harness/` 内容
5. 完成后可移除或替换本占位文件

## 说明

此占位用于驱动 setup 之后的项目内本地补充流程。
