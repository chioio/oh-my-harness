---
"@chioio/oh-my-harness": minor
---

Add update support and committable harness mode.

The CLI now supports refreshing managed files with `update` and can generate a committable shared harness with `--committable`.
